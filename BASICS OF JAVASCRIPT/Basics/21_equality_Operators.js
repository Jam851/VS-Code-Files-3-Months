/*
'==' is double equals or Abstract equality.
'===' is triple equals or Strict equality.

Both check the type of (with typeof) the operands on the left & right to them in there first step.
What they do after that is what differentiates them from each other.
*/